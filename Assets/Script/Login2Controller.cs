using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Login2Controller : MonoBehaviour
{
    // Start is called before the first frame update
    public ShowUi showUi;
    void Start()
    {
        showUi.changUIState();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
